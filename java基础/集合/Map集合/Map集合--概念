java.util.Map<K,V>集合
Map集合的特点：
      1.Map集合是一个双列集合，一个元素包含两个值(一个为key，一个为value)
      2.Map集合中的元素，key和value的数据类型可以相同，也可以不同 
      3.Map元素中的元素，key是不允许重复的，value可以重复
      4.Map集合中的元素，key和value是一一对应的
      
常用的实现类有：
      HashMap<K,V>,以及他的子类：LinkedHashMap<K,V>
          java.util.HashMap<K,V>集合实现了Map接口，特点：
                1.集合底层是哈希表，查询速度非常快
                2.该集合是一个无序的集合，存储元素和取出元素有可能不一致
          
          java.util.LinkedHashMap<K,V>集合 extends HashMap<K,V>集合，特点：
                1.LinkedHashMap集合底层是哈希表+链表，保证迭代的顺序
                2.LinkedHashMap集合是一个有序的集合，存储元素和取出元素的顺序是一致的
